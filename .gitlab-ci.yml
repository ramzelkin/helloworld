
image: node:14.16.0

variables:
  jobId: CI_JOB_ID

pages:
  stage: deploy
  cache:
    key: jobId
  before_script:
    - git submodule sync --recursive && git submodule update --remote --init
    - npm install
    - node Test.js
  script:
    - chmod +x ./Build.sh
    - ./Build.sh
    - cd project-help
    - node buildAssets.js
    - cd ..
    - chmod +x ./saveHash.sh
    - ./saveHash.sh
    - chmod +x ./afterBuild.sh
    - ./afterBuild.sh
  artifacts:
    paths:
      - public
  only:
    - master
